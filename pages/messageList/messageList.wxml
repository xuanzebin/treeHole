<!--pages/messageList.wxml-->
<view wx:for="{{messageList}}" wx:key="{{key}}" class="messageList">
  <text class="userName">{{item.userName}}:</text>
  <text class="content">{{item.content}}</text>
</view>
  